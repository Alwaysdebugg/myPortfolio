/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: '/myPortfolio',
  basePath: '',
  output: 'export',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig 